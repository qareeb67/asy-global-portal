import { useEffect, useState } from 'react';
import { api } from '../services/api';

export default function Activity(){const [items,setItems]=useState([]);const [error,setError]=useState('');useEffect(()=>{api.get('/activity').then(({data})=>setItems(data.activities)).catch(err=>setError(err.response?.data?.message||'Unable to load activity.'));},[]);return <div><div className="page-heading"><div><span className="eyebrow">AUDIT TRAIL</span><h2>Activity log</h2><p>See who changed or accessed records.</p></div></div>{error&&<div className="error-box">{error}</div>}<div className="table-card"><table><thead><tr><th>Time</th><th>User</th><th>Action</th><th>Entity</th><th>Details</th></tr></thead><tbody>{items.map(a=><tr key={a.id}><td>{new Date(a.created_at).toLocaleString()}</td><td>{a.user_name||'System'}</td><td><span className="role-badge">{a.action}</span></td><td>{a.entity_type||'—'} {a.entity_id?`#${a.entity_id}`:''}</td><td><code className="json-preview">{JSON.stringify(a.details||{})}</code></td></tr>)}{!items.length&&<tr><td colSpan="5" className="empty-cell">No activity yet.</td></tr>}</tbody></table></div></div>}
